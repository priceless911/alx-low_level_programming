#include "main.h"
/**
 * add - prints all possible combinations of two two-digit numbers
 * @i: first digit
 * @j: second digit
 * Return: 0 (Success)
 */
int add(int i, int j)
{
	int sum;

	sum = i + j;
	return (sum);
}
